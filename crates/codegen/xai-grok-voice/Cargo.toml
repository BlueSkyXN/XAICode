[package]
license = "Apache-2.0"
name = "xai-grok-voice"
version = "0.1.0"
edition.workspace = true
description = "Optional provider-neutral voice dictation adapter"

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
toml = { workspace = true }
[features]
default = []
# Compatibility-only feature names retained for existing build invocations.
# The source-clean crate contains config and protocol carriers, not capture or
# network runtime implementations.
default-bazel = []
audio = []
[lints]
workspace = true
